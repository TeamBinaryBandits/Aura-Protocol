# Keep false in production until a generated Midnight client is wired to the 1AM connector.
# Local Vite development enables the clearly labelled demo flow automatically.
VITE_AURA_DEMO_MODE=false

# DUST that remains after a transaction estimate. This is a safety buffer, not a fee.
VITE_MIDNIGHT_DUST_BUFFER=30
