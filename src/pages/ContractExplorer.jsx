import React from 'react';
import { Copy, FileCode2, ShieldCheck } from 'lucide-react';
import { listKnownSurveys } from '../services/survey-registry';

export default function ContractExplorer() {
  const surveys = listKnownSurveys();
  return <div className="relative z-10 max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-10 space-y-7"><section className="p-7 rounded-3xl bg-white border border-emerald-200 shadow-sm space-y-3"><div className="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-emerald-100 text-emerald-900 text-xs font-mono"><FileCode2 className="w-3.5 h-3.5" />Compiler artifacts</div><h1 className="text-3xl font-extrabold text-slate-900">Live contract registry</h1><p className="text-sm text-slate-600 leading-relaxed">AURA has no address reservation endpoint. A listed contract has a finalized address returned by Midnight.js or was explicitly configured as a finalized Vercel registry entry.</p></section>{!surveys.length ? <div className="p-10 rounded-3xl bg-white border border-dashed border-slate-300 text-center text-sm text-slate-600">No finalized contract has been registered. Deploy a survey with 1AM to add one.</div> : <div className="space-y-4">{surveys.map((survey) => <article key={survey.contractAddress} className="p-6 rounded-3xl bg-white border border-slate-200 shadow-sm space-y-3"><div className="flex justify-between gap-3"><div><h2 className="font-bold text-slate-900">{survey.title}</h2><p className="text-xs text-slate-500 mt-1 uppercase font-mono">{survey.network}</p></div><ShieldCheck className="w-5 h-5 text-emerald-700" /></div><div className="p-3 rounded-xl bg-slate-50 border border-slate-200 text-xs font-mono break-all text-slate-700">{survey.contractAddress}</div>{survey.deploymentTxHash && <div className="p-3 rounded-xl bg-slate-50 border border-slate-200 text-xs font-mono break-all text-slate-700">Deployment hash: {survey.deploymentTxHash}</div>}<button type="button" onClick={() => navigator.clipboard.writeText(survey.contractAddress)} className="inline-flex items-center gap-1.5 text-xs font-semibold text-emerald-800"><Copy className="w-3.5 h-3.5" />Copy address</button></article>)}</div>}</div>;
}
