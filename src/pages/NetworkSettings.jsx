import React, { useEffect, useState } from 'react';
import { AlertCircle, CheckCircle2, ExternalLink, Gauge, Globe, LogOut, ShieldCheck, Wallet } from 'lucide-react';
import { ESTIMATED_TRANSACTION_DUST, MIN_DUST_BUFFER, midnightService, NETWORKS, ONE_AM_INSTALL_URL } from '../services/midnight';

const cards = [NETWORKS.PREVIEW, NETWORKS.PREPROD];

export default function NetworkSettings() {
  const [wallet, setWallet] = useState({ isConnected: false, network: NETWORKS.PREVIEW, walletAddress: null, dustBalance: null, nightBalance: null, lastError: null });
  const [connecting, setConnecting] = useState(false);
  useEffect(() => midnightService.subscribe(setWallet), []);
  const connect = async () => { setConnecting(true); await midnightService.connectWallet(); setConnecting(false); };
  const balance = (amount, token) => typeof amount === 'number' ? `${amount} ${token}` : 'Not reported by 1AM';
  const needed = ESTIMATED_TRANSACTION_DUST + MIN_DUST_BUFFER;

  return <div className="relative z-10 max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-10 space-y-7"><section className="max-w-2xl space-y-3"><div className="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-emerald-100 text-emerald-900 text-xs font-mono"><Globe className="w-3.5 h-3.5" />Network & wallet readiness</div><h1 className="text-3xl font-extrabold text-slate-900">Connect 1AM safely</h1><p className="text-sm text-slate-600 leading-relaxed">The wallet chooses the node and indexer configuration. AURA validates its network, uses its indexer URLs, and cannot manufacture an account or submit outside your approval.</p></section><section className="grid md:grid-cols-2 gap-5">{cards.map((network) => { const active = wallet.network.id === network.id; return <article key={network.id} className={`p-6 rounded-3xl border ${active ? 'bg-white border-emerald-500' : 'bg-white/70 border-slate-200'}`}><div className="flex justify-between gap-3"><div><h2 className="font-bold text-slate-900">{network.name}</h2><p className="mt-2 text-xs leading-relaxed text-slate-600">{network.id === 'preview' ? 'Get Preview tNIGHT, register it in 1AM, then wait for tDUST generation.' : 'Use the current Midnight token guide; no unverified faucet is listed.'}</p></div>{active ? <CheckCircle2 className="w-5 h-5 text-emerald-600" /> : <button type="button" onClick={() => midnightService.setNetwork(network.id)} className="px-3 py-1.5 h-fit rounded-lg bg-slate-100 text-xs font-semibold">Select</button>}</div><div className="mt-5 flex gap-2 flex-wrap">{network.faucetUrl && <a href={network.faucetUrl} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1.5 px-3 py-2 rounded-xl bg-sky-50 text-sky-900 text-xs font-semibold">Open Preview faucet <ExternalLink className="w-3.5 h-3.5" /></a>}<a href={network.tokenGuideUrl} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1.5 px-3 py-2 rounded-xl bg-slate-50 text-slate-700 text-xs font-semibold">Token guide <ExternalLink className="w-3.5 h-3.5" /></a></div></article>; })}</section><section className="p-7 rounded-3xl bg-white border border-slate-200 shadow-sm space-y-5"><div className="flex flex-col sm:flex-row gap-4 sm:justify-between"><div><h2 className="font-bold text-slate-900 flex gap-2 items-center"><Wallet className="w-5 h-5 text-emerald-700" />1AM DApp Connector</h2><p className="mt-1 text-xs text-slate-600">{wallet.isConnected ? 'Connected through the actual 1AM DApp Connector.' : 'Not connected.'}</p></div>{wallet.isConnected ? <button type="button" onClick={() => midnightService.disconnectWallet()} className="inline-flex items-center gap-2 px-4 py-2.5 rounded-xl bg-rose-50 border border-rose-200 text-rose-800 text-xs font-semibold"><LogOut className="w-4 h-4" />Disconnect</button> : <div className="flex gap-2"><button type="button" disabled={connecting} onClick={connect} className="inline-flex items-center gap-2 px-4 py-2.5 rounded-xl bg-emerald-700 text-white text-xs font-semibold disabled:opacity-50"><Wallet className="w-4 h-4" />{connecting ? 'Looking for 1AM…' : 'Connect 1AM'}</button><a href={ONE_AM_INSTALL_URL} target="_blank" rel="noreferrer" className="px-3 py-2.5 rounded-xl bg-slate-100 text-xs font-semibold">Install</a></div>}</div><div className="grid sm:grid-cols-3 gap-3 text-xs"><div className="p-4 rounded-2xl bg-slate-50 border border-slate-200"><p className="font-mono text-slate-500">Account</p><p className="mt-1.5 font-semibold break-all">{wallet.walletAddress || 'Connect 1AM'}</p></div><div className="p-4 rounded-2xl bg-slate-50 border border-slate-200"><p className="font-mono text-slate-500">Available DUST</p><p className="mt-1.5 font-semibold">{balance(wallet.dustBalance, 'DUST')}</p></div><div className="p-4 rounded-2xl bg-slate-50 border border-slate-200"><p className="font-mono text-slate-500">NIGHT / tNIGHT</p><p className="mt-1.5 font-semibold">{balance(wallet.nightBalance, 'tNIGHT')}</p></div></div><p className="flex gap-2 p-4 rounded-2xl bg-amber-50 border border-amber-200 text-xs text-amber-950"><Gauge className="w-4 h-4 shrink-0" /><span><strong>DUST safety buffer: {MIN_DUST_BUFFER}.</strong> AURA blocks a live transaction below {needed} DUST ({ESTIMATED_TRANSACTION_DUST} estimate + buffer). The wallet's fee decision remains authoritative.</span></p>{wallet.lastError && <p role="alert" className="flex gap-2 p-3 rounded-xl bg-rose-50 border border-rose-200 text-rose-800 text-xs"><AlertCircle className="w-4 h-4 shrink-0" />{wallet.lastError}</p>}</section><p className="flex gap-2 text-xs text-slate-600"><ShieldCheck className="w-4 h-4 text-emerald-700 shrink-0" />No local-demo button or fallback transaction exists in this build.</p></div>;
}
